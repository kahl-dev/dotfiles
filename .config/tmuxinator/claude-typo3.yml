name: <%= File.basename(Dir.pwd).gsub(/^\./, '') %>
root: <%= Dir.pwd %>

startup_window: claude

windows:
  - claude:
      panes:
        - claude
  - nvim:
      panes:
        - nvim
  - build:
      panes:
        - make install
  - lazygit:
      panes:
        - lazygit

