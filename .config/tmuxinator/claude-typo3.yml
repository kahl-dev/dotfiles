name: <%= File.basename(Dir.pwd).gsub(/^\./, '') %>
root: <%= Dir.pwd %>

startup_window: claude

windows:
  - claude:
      panes:
        - claude
  - git-nvim:
      panes:
        - nvim
  - build:
      panes:
        - # Empty window ready for build commands
  - lazygit:
      panes:
        - lazygit