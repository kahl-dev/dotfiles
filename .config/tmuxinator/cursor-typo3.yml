name: <%= File.basename(Dir.pwd).gsub(/^\./, '') %>
root: <%= Dir.pwd %>

startup_window: cursor

windows:
  - cursor:
      panes:
        - cursor
  - nvim:
      panes:
        - nvim
  - build:
      panes:
        - make install
  - git:
      panes:
        - git
