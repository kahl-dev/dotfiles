# Claude Code Performance Optimizations
# Only loaded when CLAUDECODE=1 is set
# Provides fast tool aliases without personal configuration

# Replace slow tools with fast alternatives
alias grep='/opt/homebrew/bin/rg --smart-case'
alias find='/opt/homebrew/bin/fd'

# Optional: Add sd for simpler text replacement
alias sed='/opt/homebrew/bin/sd'

# That's it - minimal, focused, fast