# Pi (badlogic/pi-mono) agent harness
# Long prompt-cache retention: 1h tier on Anthropic, 24h on OpenAI.
# Only affects direct API provider URLs; subscription providers ignore it.
export PI_CACHE_RETENTION=long
