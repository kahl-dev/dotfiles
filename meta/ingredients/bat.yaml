- defaults:
    link:
      relink: true
      force: true
      relative: false

- clean: ['~']

- create:
    - ~/.config

- shell:
  - mkdir -p ~/.local/bin && ln -s /usr/bin/batcat ~/.local/bin/bat
