- defaults:
    link:
      relink: true
      create: true
      force: true
    shell:
      quiet: false
      stderr: true

- clean: []

- link:
    ~/.cursor: ./.cursor

- shell:
    - description: "Install Cursor CLI if not present"
      command: command_exists cursor-agent || curl https://cursor.com/install -fsS | bash
