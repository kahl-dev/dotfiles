- defaults:
    link:
      relink: true
      force: true
      relative: false

- clean: ['~']

- create:
    - ~/.config

- shell:
  - sudo apt install bat tmux vim fzf zoxide curl btop ripgrep gettext unzip
